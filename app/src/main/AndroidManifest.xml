<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.bcompanion3">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Authentication" />
        <activity android:name=".SignUp" />
        <activity android:name=".SignIn" />
        <activity android:name=".CheckPhoneNumber" />
        <activity android:name=".ContactUs" />
        <activity android:name=".map.BalloonOverlayActivity" />
        <activity
            android:name=".Places"
            android:label="@string/title_activity_place"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AboutUs" />
        <activity
            android:name=".Place"
            android:label="@string/title_activity_place"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".CreateGroup"
            android:label="Новая группа"/>
        <activity
            android:name=".ChatActivity"
            android:label="Чат"/>
    </application>


</manifest>